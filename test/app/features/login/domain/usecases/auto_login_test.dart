import 'package:flutter_test/flutter_test.dart';

main() {
  group('success', () {
    test(
      'should return a UserEntity if there is a user saved in the device',
      () async {
        // arrange

        // act

        // assert
      },
    );
  });

  test(
    "should return null if there is no User saved in the device's memory",
    () async {
      // arrange

      // act

      // assert
    },
  );
}
